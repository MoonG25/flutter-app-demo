import 'package:bone_app/components/neon_text.dart';
import 'package:flutter/material.dart';

class ReservationView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: NeonText('Reservation View'),
    );
  }
}
